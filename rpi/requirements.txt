# Raspberry Pi OS Bookworm (Pi 5). Recomendado:
#   sudo apt install -y python3-opencv python3-numpy python3-flask python3-serial
#   (y crear el venv con --system-site-packages si usas venv)
#
# Si prefieres instalar todo con pip:
numpy>=1.24
opencv-python>=4.8
Flask>=2.3
pyserial>=3.5
# Opcional: boton fisico de arranque en un GPIO de la Pi
gpiozero>=2.0
